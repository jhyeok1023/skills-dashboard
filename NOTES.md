# NOTES

## 현재 상태

- 구성: Go 백엔드(단일 바이너리, 웹 UI 임베드) + SvelteKit SPA. 데이터 소스는 CloudWatch 단일. `mise`가 툴체인과 빌드 커맨드를 관리.
- 미해결:
  - 팟 애플리케이션 로그의 최종 형식 미확정. 현재 필드 매핑은 참고 구현의 실제 샘플에서 역산한 기본값이며, 설정 화면의 "샘플 붙여넣기 → 파싱 미리보기"로 확인 후 조정 필요.
  - 팟 min/max는 AWS API만으로 얻을 수 없어 구간 내 관측값을 사용. 정확한 HPA 값이 필요해지면 K8s API 도입 여부를 다시 결정해야 함.
  - 실제 AWS 계정에 대한 엔드투엔드 확인 미실시(자격증명 없음). 모든 검증은 인터페이스 목킹과 픽스처 기반. WAF 브레이크다운의 `max(@timestamp) as lastTs`는 AWS 문서상 지원되는 stats 함수지만 실제 로그 그룹에서 실행해 보지 않았다.
  - `InsightsRunner`의 `Timeout`/`Concurrency`가 `once.Do` 안에서 굳어, 설정 화면에서 `queryTimeoutSeconds`를 바꿔도 재시작 전까지 반영되지 않는다.

## 결정 로그
<!-- append -->

### 2026-08-16 아무도 읽지 않는 그룹 키가 총계를 갉아먹고 있었다

- 맥락: "팟 로그의 404·403도 WAF처럼 각각 자세히 보기가 있어야 한다"에서 출발해 두 가지가 나왔다. ⓐ `pod-status-codes`는 상세 컬럼을 `userAgentField`가 설정된 경우에만 붙였는데(`panels_logs.go`), 그 필드는 기본값이 없다 — 액세스 로그에 User-Agent가 있는지는 애플리케이션이 정하므로 짐작할 이름이 없기 때문이다. 결과적으로 **기본 설치에서는 팟 로그에 펼치기가 하나도 없었다**. ⓑ 코드별 드릴다운이 아예 없었다. 코드별 수치는 차트 시리즈로만 존재했다. 그리고 그걸 만들려고 쿼리를 열었더니 **`PodBadStatusSeries`가 `by bin(t), status, path`로 묶고 있는데 `path`를 읽는 코드가 없었다.** Insights는 stats 결과를 `InsightsMaxRows`(10,000)에서 자르고 그 사실을 결과에 적지 않는다. 행 수는 구간 수 × 코드 수 × 경로 수이므로 2시간·5분이면 경로 83종에서 시계열이 잘린다 — 랜덤 URL을 긁는 스캐너 하나면 넘는다. 즉 쓰지도 않는 그룹 키가 차트와 그 옆의 `비정상 응답` 총계를 조용히 줄이고 있었다. `awsx.QueryResult.Truncated`는 이미 계산되고 있었지만 **아무도 읽지 않았다**.
- 채택: 네 가지. ⓐ 시계열에서 `path` 그룹 키를 제거한다 — 행 수가 구간 × 코드로 돌아가 총계가 정직해진다. ⓑ 그 집계는 `PodBadStatusByPath`로 옮긴다. `bin()`이 없으므로 같은 경로들이 (코드, 경로)당 한 행으로 줄고, 그래서 **`limit` 절 없이** 감당된다. ⓒ 새 패널 `pod-status-breakdown`(코드당 한 행, 상위 경로는 행의 상세)을 `pod-logs`에만 붙인다. ⓓ `warnIfTruncated`가 `Truncated`를 읽어 패널 경고로 낸다 — 잘린 집계는 작은 답이 아니라 틀린 답이고, 남은 행은 쿼리가 먼저 돌려준 것들이라 얼마나 모자란지 알 수 없다. 상세 컬럼은 `container`·`namespace`로 무조건화했다: k8s 엔벨로프 필드라 설정과 무관하게 값이 있으므로 `—`만 뜨는 상세를 열 수 없다.
- 기각: ⓐ 시계열에 `path`를 남긴 채 재사용 — 스캔은 하나로 끝나지만 경로 83종에서 차트가 잘린다. 스캔 하나를 아끼려고 총계를 포기하는 거래다. ⓑ `sort n desc | limit N`으로 서버에서 자르기 — (코드, 경로) 볼륨 순으로 자르므로 404 홍수가 드문 403 행을 결과에서 통째로 밀어낸다. 그 행이 이 패널의 존재 이유다. WAF에서 `actionFanout`이 헤드룸으로 막은 것과 같은 함정이고, 여기서는 자르기를 Go로 옮기는 쪽이 더 싸다. ⓒ 두 패널이 같은 캐시 키를 공유하게 해 스캔 하나로 맞추기 — `Cache.Do`가 single-flight이니 동작은 하지만, 두 빌더가 쿼리 집합을 글자 단위로 맞춰야만 성립하고 그 조건이 깨져도 조용히 스캔이 하나 늘 뿐 아무것도 실패하지 않는다.
- 대가: `pod-logs` 페이지의 Insights 쿼리가 2건에서 3건으로 늘었다. 개요는 그대로다 — `pod-status-breakdown`을 `pages["overview"]`에 넣지 않았다. 그리고 코드별 상위 경로는 한 행 안의 문자열 하나(`topPaths`)라 중첩 표가 아니다. 상세는 그 행의 값 목록이고 경로 목록은 그 중 하나일 뿐이며, `topN`에서 자르면 `외 N개`를 붙인다 — 조용히 자르면 "이게 그 경로들"로 읽힌다.
- 곁다리: `TestOnlyPerRequestTablesDeclareDetailColumns`의 이름과 의도를 고쳤다. 규칙은 "개별 요청 표만"이 아니라 "행이 자기 자신보다 더 말할 게 있는 표만"이다 — 상태 코드 행의 경로 목록은 셀에 들어가지 않는다.

### 2026-08-14 모션은 역할별 토큰으로, 폴링은 보이는 탭에서만, 툴팁은 프레임당 한 번

- 맥락: 체감 반응성 감사. 큰 위반은 세 가지였다 — ⓐ 차트 툴팁이 mousemove마다 텍스트를 갈아치운 직후 `offsetWidth`를 읽어 강제 동기 레이아웃을 만들고, `cursor.sync`가 이를 페이지의 모든 차트에 복제했다. ⓑ `visibilitychange` 처리가 전혀 없어 숨긴 탭에서도 자동 새로고침(유료 Insights 스캔)과 트래픽 점검(프로덕션 GET)이 계속 나갔다 — "바이트당 과금이라 기본 꺼짐"을 택한 코드가 배경 탭은 막지 않고 있었다. ⓒ 새로고침 중 화면에 낡은 데이터가 낡았다는 표시 없이 남았다(창 변경 후 최대 90초).
- 채택: ⓐ 툴팁을 rAF 1회로 병합하고, 텍스트 재작성과 측정은 커서가 샘플 경계를 넘을 때만 한다(`renderedIdx`) — 사이 프레임은 transform 한 줄. swatch·아이콘은 시리즈 배열 단위 WeakMap 캐시. 리사이즈는 trailing 100ms 디바운스. ⓑ `visibleInterval`(lib/visibility.ts) 하나로 세 인터벌을 감쌈 — 숨김 중 틱은 건너뛰고, 놓친 게 있으면 복귀 즉시 1회. ⓒ 그리드에 `aria-busy` + opacity 0.6, 복귀는 무전환(새 데이터의 등장 자체가 피드백). duration/easing은 tokens.css의 역할별 토큰(`--dur-*`, `--ease-*`)으로 모으고, StatTile의 JS 300ms 타이머는 `animationend`로 대체해 flash 길이의 출처를 CSS 한 곳으로 줄였다. 눌림 scale(0.97)은 전부 제거하고 `button:active` 배경색으로 바꿨다 — input까지 전환 없이 snap하던 것이 조잡하다는 판단.
- 기각: ⓐ 툴팁 크기를 상수로 고정 — 내용에 따라 폭이 변해 가장자리 플립이 틀어진다. ⓑ 복귀 시 갱신 없이 다음 틱 대기 — 30분 숨겼다 돌아오면 최대 인터벌만큼 낡은 화면을 본다. reduced-motion 토큰 오버라이드 — app.css의 전역 `!important` 블록이 서드파티 CSS까지 이미 덮는다.
- 대가: layerchart 툴팁의 reduced-motion은 CSS로 막을 수 없어(`svelte/transition` fade는 JS 구동) `fadeDuration=0`을 마운트 시점에 한 번 읽어 넘긴다 — 세션 중 OS 설정 변경은 새로고침 전까지 반영되지 않는다. 툴팁이 열린 채 데이터가 갱신되면 커서가 움직이기 전까지 낡은 값이 남는 기존 동작은 그대로다(`renderedIdx` 리셋으로 악화만 방지).
- 확인 못 한 것: `e2e/check.e2e.ts:81`(대상 미설정 화면)은 이 변경 전 HEAD에서도 같은 `SyntaxError: Unexpected end of JSON input`으로 실패한다 — 픽스처의 `route.fetch()` 경로 문제로 보이며 이번 범위 밖.

### 2026-08-10 SEARCH 스키마의 차원 집합은 문서에서 확인하고, 테스트가 그 표를 들고 있는다

- 맥락: 노드 리소스와 팟 상태 패널이 처음부터 비어 있었다. 원인은 같다 — `MetricSpec.Dimensions`가 **게시되지 않는 차원 집합**이었다. `NodeResourceMetrics`는 `{ClusterName, NodeName}`인데 Container Insights가 노드 지표를 게시하는 집합은 `{NodeName, ClusterName, InstanceId}`와 `{ClusterName}` 둘뿐이고, `PodStatusMetrics`는 `{ClusterName, Namespace}`인데 `pod_status_*` 와 `pod_number_of_container_restarts` 에는 그 집합이 아예 없다(팟 단위 집합은 `{PodName, Namespace, ClusterName}`). SEARCH 스키마는 **집합으로** 매칭되므로 이름 하나가 남거나 모자라면 매칭이 0건이 된다. 그리고 `GetMetricData`는 매칭 0건을 에러가 아니라 빈 결과로 돌려준다 — 쿼리 값은 다 치르고, 화면에는 "조용한 클러스터"와 구분되지 않는 빈 차트가 남는다. `pod_cpu_utilization`은 우연히 옳은 집합이라 동작했고, 그래서 이 계열 전체가 맞는 것처럼 보였다.
- 채택: 차원을 문서 값으로 고치고(`{ClusterName, InstanceId, NodeName}`, `{ClusterName, Namespace, PodName}`), **`TestContainerInsightsDimensionsArePublishedSets`가 지표명 → 게시 집합 표를 들고 있게** 했다. 다음에 누가 차원을 지어내면 실패한다. 스키마 순서는 무관하므로 정렬해서 비교한다. 팟 상태는 `PodName`으로 팬아웃하지만 빌더가 이미 `sumSeries`로 접으므로 표시되는 숫자의 의미는 그대로다.
- 함께 처리: `pod_status_*` 는 **확장 관찰성에서만** 게시된다. 차원을 고쳐도 구버전 에이전트에서는 비므로, 빈 경우에 그 애드온을 지목하는 경고를 붙였다. 이유를 말하지 않는 빈 패널이 이 버그를 일 년쯤 숨길 수 있었던 조건이다.
- 기각: ⓐ 노드를 `{ClusterName}` 집합으로 읽기 — 쿼리 하나로 끝나지만 클러스터 전체 평균 한 줄이 되어 "어느 노드가 뜨거운가"를 못 본다. 그 질문이 이 패널의 존재 이유다. ⓑ 차원을 설정값으로 빼기 — 틀린 값을 설정 화면에서도 똑같이 틀리게 넣을 수 있고, 그때는 테스트조차 없다.
- 대가: 표가 하나 늘었고 새 Container Insights 지표를 추가할 때마다 손으로 채워야 한다. 그게 요점이다 — 채우려면 문서를 열어야 한다.

### 2026-08-10 리소스 패널은 무엇을 재는지로 쪼갠다

- 맥락: `팟 리소스 사용률` 한 패널이 CPU·메모리·각각의 limit 대비 넷을 한 차트에 얹었다. 시리즈 수는 팟 수 × 지표 수라 팟 20개면 80줄이 220px 위에 겹쳤다. 단위가 전부 `%`라 축은 정직했지만 읽을 수 없었다. 바로 아래 결정에서 툴팁을 10행으로 자른 것이 이 패널이며, 그건 증상 쪽 처치였다.
- 채택: 패널을 `pod-cpu` / `pod-mem` / `node-cpu` / `node-mem` / `node-disk` 로 쪼갠다. 메트릭 키가 이미 `pod.cpu` · `pod.cpu.limit` 로 겹쳐 있어 `SpecsWithPrefix` 한 줄이 정확히 갈라 주고, `buildResourcePanel`은 그대로 재사용한다. **프론트엔드는 손대지 않는다** — 라우트는 `pageId`만 넘기고 패널 구성은 서버가 정하므로, 이 종류의 변경은 백엔드 한 곳에서 끝나야 맞다.
- 기각: ⓐ `Series`에 `group`을 넣고 `PanelCard`가 그룹당 차트를 그리기 — 카드 수는 그대로지만 프론트에 새 개념이 생기고, stat 줄은 여전히 섞인 채로 남는다. ⓑ limit 대비 지표를 빼서 시리즈를 절반으로 — 겹침의 원인은 지표 수가 아니라 종류가 섞인 것이라 팟이 늘면 그대로 돌아온다.
- 대가: 쿠버네티스 화면의 카드가 4개에서 7개로 늘었다. SEARCH 쿼리 수는 그대로다(같은 스펙을 나눠 담을 뿐).
- 곁다리: `buildCountsPanel`이 `cluster_failed_node_count`를 **조회하고 버리고 있었다** — `specs[0]`, `specs[1]`만 읽고 `results["count.nodes.failed|cluster"]`는 저장소 어디에서도 읽히지 않았다. 개요·쿠버네티스를 열 때마다 SEARCH 하나를 값 없이 태운 셈이다. stat과 시리즈를 붙이고, 위치 인덱싱을 키 조회로 바꿨다.

### 2026-08-10 취소된 요청의 에러는 캐시하지 않는다

- 맥락: "15m/30m/1h/2h 가 될 때도 있고 안 될 때도 있다"의 원인. `Cache.Do`는 키를 선점한 첫 호출자의 `ctx`로 `load(ctx)`를 돌리는데(`cache.go:100`), 그 호출자의 브라우저 요청이 취소되면 `context canceled`가 `errorTTL`(5초) 동안 **캐시된 실패**로 남고, 뒤이은 요청이 `<-e.done`를 기다렸다가 그 취소를 그대로 받았다(`cache.go:70-83`). `grep -rn "context.Canceled" internal/`은 아무것도 잡히지 않았다 — 가드가 없었다. 취소는 매번 보장돼 있었다: `PageView`의 로드 이펙트가 `untrack` 밖에서 `isValid`를 부르고 그 안이 `timeRange.ranges`를 읽는데, `/api/meta` 응답의 `applyMeta`가 그 배열을 통째로 갈아끼우므로 **마운트마다 같은 range/period로 두 번째 요청**이 나가고 그 첫 줄이 `controller.abort()`였다. 캐시 키는 range 문자열이 아니라 윈도를 담고 `NewWindow`가 end를 period 경계로 내리므로 period=1m이면 1분 동안 키가 바이트 단위로 동일하다 — 기본 period가 1m인 range가 정확히 15m·30m·1h·2h다.
- 채택: 세 군데. ⓐ `load` 후 `e.err != nil && ctx.Err() != nil`이면 엔트리를 만료시키지 않고 **키에서 지운다**. 기다리던 호출자는 `c.entries[key] == e`가 깨져 다시 적재한다. 취소는 데이터에 대한 사실이 아니라 호출자에 대한 사실이므로 공유될 값이 아니다. ⓑ `isValid` 호출을 `untrack`으로 감싸 중복 요청을 없앤다. ⓒ `handlePage`에 90초 데드라인을 둔다 — 패널이 순차로 만들어지고 WAF 페이지는 Insights 웨이브가 세 번이라, 합이 `WriteTimeout` 120초를 넘으면 Go가 커넥션을 끊고 브라우저에는 전체 빨간 카드가 떴다. 데드라인이 먼저 걸리면 `noteQueryErrors`가 어느 쿼리가 느렸는지를 패널별 경고로 남긴다.
- 기각: ⓐ `ErrorTTL`을 0으로 — 스로틀된 의존성을 매 틱 전속력으로 재시도하게 된다. 그 캐싱은 옳고, 취소만 옳지 않았다. ⓑ 프론트에서 abort 를 안 하기 — 범위를 바꿀 때 낡은 응답이 늦게 도착해 덮어쓴다. ⓒ `WriteTimeout` 을 올리기 — 상한을 미루기만 하고, 여전히 이유 없는 실패로 끝난다.
- 대가: 근본 수정(ⓐ)은 캐시에 있고 나머지 둘은 이 경로를 얼마나 자주 밟는지의 문제다. 셋 다 필요하지만 회귀 테스트는 ⓐ에 있다 — 빠르게 range를 눌러 대는 것만으로 ⓑ 없이도 재현되기 때문이다.
- 남은 한계: `InsightsRunner`의 `Timeout`/`Concurrency`는 `once.Do` 안에서 굳으므로 설정 화면에서 `queryTimeoutSeconds`를 바꿔도 재시작 전까지 반영되지 않는다.

### 2026-08-10 WAF 통계는 action 을 그룹 키에 넣고, 개별 요청 목록은 action 으로 거르지 않는다

- 맥락: 경로·메소드·헤더 통계가 action을 전혀 구분하지 않아 애플리케이션에 도달한 요청과 WAF가 막은 요청이 한 숫자에 섞였다. 그리고 `waf.blocked.list`는 **실행되고 매 조회마다 윈도 전체를 스캔하면서 결과는 버려지고** 있었다(직전 커밋의 `ponytail:` 부채).
- 채택: 세 브레이크다운 쿼리에 `, action`과 `max(@timestamp) as lastTs`를 더하고 Go에서 (키) 단위로 피벗해 허용/차단/기타/합계/마지막 처리를 낸다. Insights에는 조건부 집계가 없어 "action=BLOCK인 것만 세기"를 한 stats로 쓸 수 없고, 두 stats로 쓰면 같은 바이트를 두 번 스캔한다 — 그룹 키를 늘리는 쪽이 스캔 하나다. 버려지던 목록 쿼리는 `RecentList`로 바꿔(action 필터 제거, `action` 필드 추가) `waf-traffic` 패널의 표로 렌더한다. 페이지 전체의 Insights 쿼리 수는 그대로다.
- 기각: ⓐ 목록을 `waf-blocked`에 그대로 두고 렌더만 하기 — 차단만 나열하면 빈 목록이 "차단이 없었다"인지 "트래픽이 없었다"인지 구분되지 않는다. 그 구분이 이 목록을 만드는 이유였다. ⓑ 새 패널 `waf-recent` 를 만들기 — `table.total`을 독립적으로 셀 방법이 없어 행 수를 전체 건수로 표시하게 된다. `waf-traffic`은 이미 action 시계열 합계를 갖고 있어 그 계약을 공짜로 지킨다.
- 대가: 그룹 키에 action이 붙으면 행 수가 action 종류만큼 불어나므로 쿼리 limit을 `topN * 4`로 올렸다. 안 올리면 쿼리의 limit이 (키, action) 볼륨 순으로 자르므로, ALLOW가 큰 경로의 작은 BLOCK 행이 조용히 빠질 수 있다 — 그건 이 표가 존재하는 이유인 바로 그 숫자다. 그리고 `waf-traffic`이 표를 갖게 되면서 개요 화면에서도 전폭을 쓴다.
- 함께 지불한 비용: **개요 화면의 Insights 스캔이 하나 늘었다.** `waf-traffic`이 개요에도 올라가 있고(`server.go`의 `pages`), 패널은 어디서든 같은 것을 보여준다는 규칙 때문에 "개요에서는 목록을 빼기"가 불가능하다. WAF 화면은 그대로(하나 빼고 하나 넣음), 개요만 +1이다. 개요에서 목록이 값어치를 못 한다고 판단되면 고르는 수는 목록을 조건부로 빼는 것이 아니라 `pages["overview"]`에서 `waf-traffic`을 빼는 것이다.

### 2026-08-10 툴팁은 값 순으로 자르고, 자른 것을 말한다

- 맥락: 팟 리소스 패널은 팟 수 × 지표 수만큼 시리즈를 만든다. 팟 20개면 40행이 220px 차트 위에 뜨고, 위치 보정이 `Math.max(0, …)`로 클램프하므로 툴팁이 차트를 그대로 덮었다. `tooltipRows`에 상한이 없었다.
- 채택: 커서 지점 값 내림차순으로 정렬해 상위 10행만 내고 잘린 개수를 함께 돌려준다. 도착 순서대로 자르면 "백엔드가 먼저 나열한 것"이 남지, 지금 볼 만한 것이 남지 않는다. 결측은 마지막으로 정렬한다 — "측정되지 않음"은 작은 값이 아니다. 잘렸으면 `외 N개`를 표시한다: 조용히 40개 중 10개를 보여주면 "이게 그 팟들"로 읽힌다.
- 함께 처리: 행 수만 묶어서는 부족했다. 긴 팟 이름이 세 줄로 감싸 10행이 370px가 됐다. 툴팁 안에서만 라벨을 한 줄로 자른다 — 대시보드에서 값을 자르는 유일한 자리이며, 전체 이름은 바로 아래 범례에 잘리지 않고 그대로 있다.
- 기각: 툴팁에 `max-height`와 스크롤 — 툴팁은 `pointer-events: none`이라 스크롤할 수 없다.
- 곁다리: `UPlotChart.svelte`에 리터럴 `\x00`/`\x01`이 박혀 있어 git이 이 파일을 바이너리로 취급하고 있었다(`Bin 6661 -> 14508 bytes`). 이스케이프로 바꿔 diff를 되살렸다.

### 2026-08-10 패널 확대를 다시 들이되, 축소판과 확대판이 갈라질 자리를 없앤다

- 맥락: 확대 다이얼로그는 이전 구현에 있었고 **일부러 뺐던** 기능이다(`PanelCard.svelte` 상단 주석). 그리드에서 막대 8개, 확대에서 30개를 보여 주고 막대 눈금을 화면에 있는 것에서 계산했기 때문에 같은 패널이 두 곳에서 다르게 읽혔다.
- 채택: 카드와 모달이 **snippet 하나**를 렌더하고 인자는 차트 높이뿐이다. 행 수나 눈금이 갈라질 코드 경로가 없다. 모달은 네이티브 `<dialog>` + `showModal()` — ESC, 배경, 포커스 트랩, 배경 비활성화가 전부 플랫폼에서 온다. 배경 클릭 닫기만 한 줄 붙였다.
- 함께 처리: 닫힌 `<dialog>`도 자식을 렌더하므로 `{#if open}`으로 감쌌다. 없으면 모든 패널의 통계·차트·표가 **항상 두 벌** 만들어진다 — 로그 표는 300행이라 보이지도 않는 300행을 매 갱신마다 따라 갱신하게 된다.
- 대가: 열고 닫을 때 snippet이 재생성되므로 그 패널의 표 필터·정렬·"더 보기" 위치와 uPlot 인스턴스가 초기화된다. 유지하려면 그 상태를 `DataTable`/`UPlotChart` 밖으로 끌어올려야 하는데, 확대 버튼 하나가 감당할 복잡도를 넘는다.
- 확인: 열린 채로 자동 새로고침이 계속 도는지 e2e가 확인한다(`page.clock`으로 30초를 넘긴다). 갱신을 멈추는 모달은 낡은 숫자를 낡았다는 말 없이 읽게 한다. 새로고침 **버튼**은 열려 있는 동안 누를 수 없는데, 그건 `showModal()`이 배경을 비활성화하는 것이고 의도대로다.

### 2026-08-10 트래픽 점검만 AWS 밖으로 나간다

- 맥락: "정상 트래픽이 제대로 들어가는지" 확인할 방법이 없었다. CloudWatch는 몇 분 늦고, 패널이 비었을 때 "트래픽이 없었다"와 "서비스가 죽었다"를 구분해 주지 않는다. 물어봐야 알 수 있는 질문이다.
- 채택: `POST /api/check` 하나. 설정된 주소로 GET 한 번, 상태 코드와 소요 시간만 돌려준다. 이것이 대시보드가 AWS 아닌 곳에 접근하는 유일한 경로이므로 경계를 좁게 잡았다 — scheme은 `http`/`https`만, 저장은 거부하고 로드는 그 값만 비우는 기존 규칙 그대로, 응답 본문은 읽고 버리고(커넥션 재사용), 이력은 메모리에 20회. 실패한 점검은 **200 + `ok:false`**로 돌려준다: 엔드포인트가 실패한 것과 대상이 실패한 것은 다른 사실이고, 합치면 대시보드 버그가 장애처럼 보인다.
- 기각: ⓐ 상단 바 상태 배지 — 매 페이지 로드마다 요청을 쏘게 되고, 그건 아무도 요청하지 않은 트래픽이다. 화면 안의 반복 주기도 기본 꺼짐이다. ⓑ CloudWatch 값(WAF ALLOW / 팟 2xx)에서 "정상/이상"을 판정해 배지로 띄우기 — 새 경로 없이 끝나지만, 지연도 "게시되지 않음"과 "죽음"의 모호함도 그대로 남는다. 물어본 것을 답으로 삼기로 했다. ⓒ 메소드·헤더·본문을 설정 가능하게 — 지금 필요한 질문은 "응답하는가" 하나다.
- 대가: README 첫 줄의 "데이터는 전부 CloudWatch에서 온다"에 예외가 하나 생겼다. 예외라고 적어 뒀다.

### 2026-08-10 저장된 설정은 읽을 때 고쳐서 쓰고, 저장할 때만 거부한다

- 맥락: 바로 아래 결정으로 `Config.Validate()`에 ALB 형식 검증을 넣었는데, `NewStore`가 `Validate()` 실패를 치명적으로 다루고 있었다(`main.go:56`이 그대로 `return err`). 예전 버전이 검증 없이 받아 저장한 값(로드 밸런서 **이름**만 넣은 경우 등)이 있으면 **부팅 자체가 막힌다.** Windows에서 탐색기로 더블클릭하면 콘솔 창이 프로세스와 함께 사라져 이유조차 안 보이고, 사용자는 `config.json`을 손으로 고쳐야 살아난다. 이건 바로 위 `main.go:76-78`에 적어 둔 원칙 — *실패는 치명적으로 두지 않고 UI가 설명하게 한다* — 을 자격증명에만 적용하고 설정에는 적용하지 않은 결과였다.
- 채택: 규칙 하나를 두 방향으로 쓴다. `inspect()`가 "무엇이 잘못됐는가 + 그것 없이 쓰려면 무엇을 버려야 하는가"를 한 목록으로 만들고, `Validate()`는 그 목록을 에러로 바꾸며(저장 경로), `Repair()`는 버리고 무엇을 버렸는지 문장으로 돌려준다(로드 경로). 규칙이 한 군데라 두 경로가 갈라질 수 없다. JSON이 깨져 파싱이 안 되면 기본값으로 시작하고 원본을 `config.json.bak`으로 옮긴다 — 그대로 두면 다음 저장이 덮어쓴다. 버린 내용은 `/api/meta`의 `notices`로 실어 설정 화면 상단에 띄운다.
- 기각: ⓐ 로드에서도 거부하되 에러 메시지를 개선 — 창이 사라지는 한 메시지는 읽히지 않고, 고칠 화면에 도달할 방법도 없다. ⓑ 로드에서 조용히 버리기 — 값이 사라진 자리는 "트래픽 없는 리소스"와 구분되지 않는다. ⓒ notices를 `/api/config`에 싣기 — 프론트가 GET 결과를 그대로 PUT으로 돌려보내는데 `DisallowUnknownFields`가 걸려 있어 저장이 깨진다.
- 함께 처리: 시작 단계에서 프로세스가 죽는 나머지 경로도 막았다. 기본 포트가 사용 중이면 8081..8085로 폴백하고(직접 지정한 포트는 존중하되 이유를 말하고 종료), 그래도 종료해야 할 때는 `GetConsoleProcessList`로 이 프로세스가 콘솔을 단독 점유하는지 보고 — 즉 더블클릭으로 뜬 창인지 보고 — 엔터를 기다린다. 터미널 실행에는 영향이 없다. `syscall.NewLazyDLL`로 호출하므로 `x/sys` 의존성은 늘지 않는다.
- 대가: 로드 경로가 관대해진 만큼, 설정 화면을 한 번도 안 여는 사용자는 값이 사라진 걸 패널이 빈 것으로만 알게 된다. 패널이 이미 "선택되지 않았습니다" 경고를 띄우므로 완전한 침묵은 아니다.

### 2026-08-10 로드 밸런서·타겟 그룹 값은 저장할 때 CloudWatch 차원으로 정규화하고, 안 되면 거부한다

- 맥락: `loadBalancer`에는 CloudWatch `LoadBalancer` 차원(`app/my-alb/50dc6c495c0c9188`)이 들어가야 하는데, 설정 화면은 안내 없는 자유 입력이었고 `Config.Validate()`에 검증이 없었다. 메트릭 SEARCH의 값 정규식(`domain.searchValueRe`)이 `:` 와 `/` 를 허용하므로 전체 ARN을 붙여넣어도 모든 검사를 통과한 뒤 아무 메트릭도 매칭하지 않는다. 결과는 에러 없는 빈 차트 — "트래픽이 없는 로드 밸런서"와 구분되지 않는다. 타겟 그룹을 자동 조회해 체크하는 경로에서만 우연히 올바른 값이 채워졌다.
- 채택: `Config.Validate()`에서 `domain.LoadBalancerDimension` / `TargetGroupDimension`으로 정규화한 뒤, 로드 밸런서가 `app/<이름>/<hex>` 꼴이 아니면 저장을 거부한다. UI 저장·손으로 고친 `config.json`·시작 시 로드가 모두 같은 경로를 탄다. 설정 화면에는 `loadbalancers` 자동 조회와 플레이스홀더를 붙였다.
- 기각: ⓐ API 계층(`handlePutConfig`)에서만 정규화 — 손으로 고친 설정 파일이 빠진다. ⓑ 경고만 하고 저장은 허용 — 조용한 빈 차트가 그대로 남는다.
- 대가: 변환 함수 세 개(`LoadBalancerDimension` / `TargetGroupDimension` / `FriendlyTargetGroupName`)를 `awsx`에서 `domain`으로 옮겼다. `awsx`가 `config`를 import하므로 `config`에서 `awsx`를 쓰면 순환이 된다. NLB/GWLB 차원은 거부된다 — 대시보드가 읽는 네임스페이스는 `AWS/ApplicationELB` 하나뿐이다.

### 2026-08-10 WAF 로그는 WAF 리전 클라이언트로 조회하고, 리전 판단은 Clients를 출처로 삼는다

- 맥락: CLOUDFRONT 스코프 웹 ACL은 us-east-1에만 로그를 남긴다. `Config.WAFRegion`(기본 `us-east-1`)과 그 리전에 핀된 `Clients.LogsGlobal`이 이미 있었는데 `LogsGlobal`은 프로덕션 코드에서 한 번도 쓰이지 않았다. Insights 러너는 `clients.Logs` 하나만 물고 있어 us-east-1 그룹 이름을 직접 넣어도 `StartQuery`가 없는 그룹으로 실패했고, 로그 그룹 자동 조회도 작업 리전만 봐서 `aws-waf-logs-*` 가 아예 목록에 없었다. 리전 판단도 두 갈래였다 — `creds.Validate()`가 빈 리전을 먼저 거르므로 `main.go`의 `cfg.Region` 폴백은 도달 불가였고(즉 `config.json`의 `region`은 클라이언트에 영향이 없었다), 그런데 WAF 메트릭 패널은 그 `cfg.Region`을 비교해 `CWGlobal` 사용 여부를 정했다.
- 채택: WAF 로그 전용 `InsightsGlobal` 러너를 두고, 러너·리전·로그 그룹을 `logSource` 한 덩어리로 묶어 패널 빌더에 넘긴다. 캐시 키에 리전을 포함해 두 리전의 동명 그룹이 서로의 결과를 받지 않게 했다. 조회 kind `waf-loggroups` 를 추가해 목록도 WAF 리전에서 읽는다. 리전 판단의 출처는 `Clients.Region` / `Clients.WAFRegion` 하나로 모으고, 시작 시 `cfg.Region`을 실제 자격증명 리전으로 맞춰 저장한다.
- 기각: ⓐ 패널마다 `if wafRegion != region` 분기 — 이미 있던 그 분기가 어긋난 원인이었다. ⓑ 리전을 설정 화면에서 편집 가능하게 — 클라이언트는 시작 시 1회 생성이라 재시작 없이는 적용되지 않는다. 읽기 전용 표시로 두고 어디서 바꾸는지 안내한다.
- 대가: 두 리전이 다르면 Insights 동시 실행 세마포어가 두 개가 된다(각 `insightsConcurrency`). 같은 리전이면 러너를 공유해 한 개로 유지한다.
- 남은 한계: `Config.WebACLs`는 ACL 이름만 저장하고 스코프를 잃는다. 그래서 WAF 메트릭 패널은 리전이 다를 때 선택된 ACL을 전부 WAF 리전에서 읽고, REGIONAL 스코프를 섞어 고르면 그쪽이 빈다. 고치려면 설정 스키마에 스코프를 넣어야 한다.

### 2026-08-10 헬스체크 경로를 팟 로그 집계에서 제외한다

- 맥락: 몇 초마다 도는 liveness/readiness 프로브가 요청 라인의 최대 공급원이라, 아무 일도 하지 않는 경로 쪽으로 응답 시간 백분위를 끌어내린다. 프로브가 실패하기 시작하면 동일한 행 수천 개가 비정상 응답 표를 채워 진짜 장애를 `LIMIT` 밖으로 밀어낸다.
- 채택: `LogFormat.ExcludePaths`(기본 `/health`, `/healthcheck`)를 두고 **Insights 집계 전**에 거른다. 차트 옆 숫자는 이미 제외한 값이 된다. 팟 로그 쿼리 5종(traffic / badStatus 집계·목록 / error 집계·목록)에 같은 조건을 적용한다. 목록과 그 옆 건수가 다른 모집단을 세지 않는다. 각 stat의 `basis`에 제외한 경로를 명시한다. 일반 `filter`는 Logs Insights 스캔량을 줄이지 않는다.
- 기각: ⓐ 응답을 받은 뒤 Go에서 거르는 안 — 스캔 비용이 그대로다. ⓑ 접두어·부분 일치 — `/healthy-users`를 함께 삼키고, 설정 미리보기(Go)와 쿼리(Insights)에서 규칙을 두 번 구현해야 해 어긋날 여지가 생긴다. 정확 일치로 고정했다.
- 대가: `/health/live` 처럼 하위 경로를 쓰면 목록에 직접 추가해야 한다.
- 주의: 필터는 `not ispresent(path) or path not in [...]` 형태여야 한다. Insights에서 없는 필드에 대한 비교는 매칭되지 않으므로, 가드 없이 쓰면 path가 없는 평문 로그가 전부 사라져 ERROR·WARN 패널이 빈다.

### 2026-08-10 로그 집계를 로컬 SQLite에서 CloudWatch Logs Insights로 옮긴다

- 맥락: 이전 구현(`C:\Users\User\source\dashboard`)은 `FilterLogEvents`로 로그 전량을 로컬 SQLite에 적재하고 로컬 SQL로 집계했다. 백분위가 인덱스 없는 `latency_ms`에 `ORDER BY … OFFSET`을 걸었고(`store.go:141-173`), WAF 헤더 통계는 `json_each` 무인덱스 크로스 조인이었으며(`store.go:315`), `SetMaxOpenConns(1)`로 수집기와 조회가 직렬화됐다(`store.go:37`). 데이터가 쌓일수록 느려지는 것이 구조적으로 필연이었다.
- 채택: 집계를 Insights로 내린다. 로컬 누적 상태를 두지 않으므로 시간이 지나도 비용이 일정하다. 조회 상한 4시간이 스캔량을 묶는다.
- 기각: 인덱스 추가·`VACUUM`·커넥션 수 확대로 기존 구조를 고치는 안. 증상은 완화되지만 "누적될수록 느려진다"는 성질 자체는 남는다.
- 대가: Insights는 스캔 바이트당 과금이라 AWS 비용이 오른다. 자동 새로고침 기본 끄기, 최소 30초, 결과 30초 캐시로 억제하고 **각 패널이 스캔량을 표시**해 비용을 눈에 보이게 한다.

### 2026-08-10 표시값은 전부 백엔드가 계산하고 프론트는 포맷팅만 한다

- 맥락: 이전 구현은 오버뷰와 상세의 같은 숫자가 달랐다. 원인은 프론트 재집계였다. `totalReq`는 `status>0` 모집단, 백분위의 `n`은 `latency_ms>0` 모집단인데 둘 다 "요청 수"로 표시됐고(`+page.svelte:143`), `errTotal`은 `LIMIT 300`인 배열의 length였으며, `Bars.svelte`가 그리드 8행·확대 30행으로 잘라 같은 패널의 분모까지 달라졌다(`+page.svelte:195`).
- 채택: 응답 계약으로 막는다. ① 화면의 모든 숫자는 `stats`에서 온다 ② `table.total`은 `rows`와 독립 계산 ③ 각 `stat`이 `basis`로 모집단을 명시 ④ 한 페이지는 요청 하나, 모든 패널이 하나의 `window` 공유 ⑤ 단일 패널 엔드포인트와 페이지 엔드포인트가 같은 빌더를 쓰고, 두 결과가 바이트 단위로 같은지 테스트가 확인.
- 기각: 프론트에서 재집계하되 헬퍼를 공유하는 안. 공유 헬퍼는 호출부가 다른 배열을 넘기는 순간 다시 갈라진다.
- 대가: 백엔드 응답이 커지고, 새 표시값을 추가할 때마다 서버를 고쳐야 한다.

### 2026-08-10 메트릭은 ListMetrics 대신 SEARCH() 수식으로 조회한다

- 맥락: 이전 구현이 결국 데이터를 못 불러온 1순위 원인. `ListMetrics`는 삭제된 팟의 차원을 약 2주간 계속 반환하므로 생성되는 쿼리 수가 단조 증가했고, `GetMetricData`의 500쿼리 상한을 넘는 순간 `ValidationError`로 **영구 실패**했다. 상한은 주석에만 있었고 강제되지 않았다(`metrics.go:29`).
- 채택: `SEARCH('{Namespace,Dims} MetricName="x" …', 'Stat', period)` 하나로 대체한다. 매칭 시리즈가 몇 개든 쿼리는 하나다. 500 상한은 청크 분할로 코드가 강제하고 테스트가 확인한다.
- 기각: `ListMetrics`에 `RecentlyActive: PT3H`만 붙이는 안. 증가 속도는 늦추지만 상한을 넘을 가능성은 남는다. (불가피하게 `ListMetrics`를 쓰는 자리에는 이 옵션을 함께 적용.)
- 대가: `SEARCH`는 반환 시리즈 수에 상한이 있고, 라벨 형식이 CloudWatch에 달려 있다.

### 2026-08-10 자격증명은 .env에서만 읽고 어디에도 쓰지 않는다

- 맥락: 액세스 키 방식이 요구사항. 브라우저에 키 입력 필드를 두면 화면·로그·스크린샷으로 새어나갈 경로가 늘어난다.
- 채택: 시작 시 `.env`에서 읽어 메모리에만 보관. 설정 화면은 그 키가 무엇으로 확인됐는지(계정·ARN·리전)만 표시한다. 로그에는 키 끝 4자리만 남긴다.
- 기각: UI 입력 + OS 키체인 암호화 저장. 플랫폼별 분기가 늘고, 로컬 도구에는 과한 복잡도.
- 대가: 키 교체는 `.env` 편집 후 재시작.

### 2026-08-10 윈도의 끝을 간격 경계로 내림한다

- 맥락: 이전 구현은 `now`를 그대로 조회 경계로 쓰고 `(ts/width)*width`로 버킷을 나눴다(`k8s.go:262`, `store.go:94`). 첫 버킷은 임의 비율로 잘리고 마지막 버킷은 항상 미완성이었다. UI는 이를 헤드라인에서 "끝에서 두 번째 버킷"을 읽는 방식으로 우회했고, 옆 차트는 마지막 버킷을 포함해 둘이 어긋났다(`+page.svelte:136`).
- 채택: `end = now.Truncate(period)`, `start = end - range`. 구간 내 모든 버킷이 완전한 버킷이 된다.
- 기각: 미완성 버킷을 포함하되 `partial` 플래그로 표시하는 안. 표시가 하나 늘 뿐 "같은 값이 두 곳에서 다르다"는 문제는 남는다.
- 대가: 최신 데이터가 최대 한 period만큼 늦게 보인다.
