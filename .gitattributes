# bin/ 의 실행 파일은 커밋해서 대회장까지 가져간다 — 거기에는 Go 툴체인이 없어
# 다시 만들 수 없기 때문이다. 개행 변환이 한 바이트만 건드려도 "올바른 Win32
# 응용 프로그램이 아닙니다" 로 나타나고, 원인이 보이지 않는다. core.autocrlf 가
# 켜진 기계에서 clone 하는 경우가 그렇다.
bin/** binary
*.exe binary
